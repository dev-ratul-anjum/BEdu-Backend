model Subject {
  id   String  @id @default(uuid())
  name String // "Mathematics"
  code String? // "MATH101"

  class_id String
  class    Class  @relation(fields: [class_id], references: [id])

  routine_id String

  results  Result[]
  routines Routine[]

  @@map("subjects")
}
