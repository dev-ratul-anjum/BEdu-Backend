model Parent {
  id    String  @id @default(uuid())
  name  String
  email String?

  userId String @unique
  user   User   @relation(fields: [userId], references: [id])

  students Student[]
  phone    Phone[]

  @@map("parents")
}
