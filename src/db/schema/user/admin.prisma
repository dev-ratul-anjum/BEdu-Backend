model Admin {
    id    String  @id @default(uuid())
    name  String
    email String? @unique

    userId String  @unique
    user   User    @relation(fields: [userId], references: [id])
    phone  Phone[]

    @@map("admins")
}
