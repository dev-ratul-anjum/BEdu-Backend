model Admin {
    id    String  @id @default(uuid())
    name  String
    email String? @unique

    user_id String  @unique
    user    User    @relation(fields: [user_id], references: [id])
    phone   Phone[]

    @@map("admins")
}
