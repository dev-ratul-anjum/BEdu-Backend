model AcademicYear {
  id        String   @id @default(uuid())
  session   String // "2024-2025"
  startDate DateTime
  endDate   DateTime
  isCurrent Boolean  @default(false)

  classes  Class[]
  exams    Exam[]
  routines Routine[]

  @@map("academic_years")
}
