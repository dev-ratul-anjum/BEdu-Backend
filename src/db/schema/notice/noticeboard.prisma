model NoticeBoard {
  id           String   @id @default(uuid())
  title        String
  content      String
  target_roles Role[] // Array of roles: ["STUDENT", "TEACHER"]
  is_archived  Boolean  @default(false)
  posted_at    DateTime @default(now())

  @@map("notices")
}
